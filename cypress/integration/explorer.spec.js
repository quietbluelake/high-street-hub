/// <reference types="cypress"/>

import Chance from 'chance';
const chance = new Chance();

describe('Explorer', () => {

    //Global variables here

    it('Can scroll around the grid to view the whole thing (depending on size)', () => {

    });
    it('Can view the grid in mixed orientation + varying screen sizes', () => {

    });
    it('Grid should be invisible', () => {

    });
    it('Can tap/click an entity to view its overview + options', () => {

    });
    it('Can access the dashboard of selected entity', () => {

    });
    it('Can exit to the Hub', () => {

    });

});