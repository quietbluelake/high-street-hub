/// <reference types="cypress"/>

import Chance from 'chance';
const chance = new Chance();

describe('Login', () => {

    //Global variables here

    it('Existing user should be able to log in', () => {

    });
    it('Existing user should be able request a "forgot password" email', () => {

    });
    it('New users should not be able to log in', () => {

    });
    it('New users should be able to create an account', () => {

    });
});