/// <reference types="cypress"/>

import Chance from 'chance';
const chance = new Chance();

describe('High Street Hub', () => {

    //Global variables here

    it('Login page should only be accessible if logged out', () => {

    });

    it('All other routes should only be accessible when logged in', () => {

    });

    it('Should stay logged in when navigating between routes', () => {

    });

});