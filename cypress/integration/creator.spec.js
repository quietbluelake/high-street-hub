/// <reference types="cypress"/>

import Chance from 'chance';
const chance = new Chance();

describe('Creator', () => {

    //Global variables here

    it('Grid should be visible', () => {

    });
    it('Can drag an entity onto a tile', () => {

    });
    it('Can use tools to modify an entity once it is connected to a tile', () => {

    });
    it('If I drag an entity onto an occupied tile, the old entity should be replaced with the new one', () => {

    });
    it('Can rotate entity', () => {

    });
    it('Can scale entity', () => {

    });
    it('Can flip entity', () => {

    });
    it('Can delete entity', () => {

    });
    it('Should not be able to access dashboard', () => {

    });
    it('Can specify custom grid size', () => {

    });
    it('Can scroll to view entire grid if needed', () => {

    });
    it('Can view grid on mixed orientations and screen sizes', () => {

    });
    it('Can filter entities in the search bar', () => {

    });
    it('Should be able to view all stored entities from Library if no filters are set', () => {

    });
    it('Can exit to Hub', () => {

    });

});